Implement the tool call planner in `tool_call_planner/planner.py`.

Follow the contract in `TASK.md`. This is the v2 version of the task: pay
special attention to the visible regression for an intent that has no matching
tool.

You may add tests and supporting files, but keep the public API exactly as
specified.

Before finishing, run the relevant tests and report the command results.
