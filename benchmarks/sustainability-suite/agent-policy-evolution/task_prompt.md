Implement the tool-call planner in `tool_call_planner/planner.py`.

The planner decides whether an AI assistant should call a tool, ask for clarification, request approval, or refuse.

Follow the contract in `TASK.md`. Keep the public API exactly as specified.

You may add tests and supporting files. Useful coverage includes ordinary success, missing arguments, policy refusal, approval, no matching tool, and prompt-injection text that tries to override structured fields or policy.

Before finishing, run the relevant tests and report the command results.
